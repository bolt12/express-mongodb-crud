extends layout
block content
  .container
    .row
      // FORM
      .col-md-5.offset-md-3
        .card
          .card-body
            form(action='/edit/'+task._id method='post')
              .form-group
                input.form-control(type='text' name='title' placeholder='Title' value=task.title)
              .form-group
                textarea.form-control(name='description' cols='80' placeholder='Add a Description')
              button.btn.btn-primary(type='submit')
                | Update
